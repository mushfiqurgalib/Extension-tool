# AI Comment Generator Extension

VS Code extension for generating code comments using AI models.

## Features

- Generate comments for selected code snippets using AI.

## Usage

1. Select code in your editor.
2. Run the command `Generate Comment with AI` from the Command Palette (`Ctrl+Shift+P` / `Cmd+Shift+P`).
